# Railway Configuration for AI Swarm Prediction Hub
# Deploy: Connect this repo to Railway, it will auto-detect services

[build]
# Railway will use the Dockerfiles in docker/

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
